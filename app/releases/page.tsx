import { ReleasesClient } from "./releases-client";

export const metadata = { title: "Release History · Qoder Live Lab", description: "Every verified market-dashboard release and the evidence behind it." };

export default function ReleasesPage() { return <ReleasesClient />; }
